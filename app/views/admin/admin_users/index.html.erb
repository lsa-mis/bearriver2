<div class="d-flex justify-content-between align-items-center mb-3">
  <h1 class="h3 mb-0">Admin Users</h1>
  <%= link_to 'New Admin User', new_admin_admin_user_path, class: 'btn btn-primary btn-sm' %>
</div>

<%= form_with url: admin_admin_users_path, method: :get, local: true, class: 'row g-2 mb-3' do %>
  <div class="col-auto">
    <%= select_tag :email, options_for_select(AdminUser.order(:email).pluck(:email), params[:email]), include_blank: 'Email', class: 'form-select form-select-sm' %>
  </div>
  <div class="col-auto">
    <%= submit_tag 'Filter', class: 'btn btn-sm btn-outline-secondary' %>
    <%= link_to 'Clear', admin_admin_users_path, class: 'btn btn-sm btn-link' %>
  </div>
<% end %>

<div class="table-responsive">
  <table class="table table-striped table-sm align-middle">
    <thead>
      <tr>
        <th>ID</th>
        <th>Email</th>
        <th>Created</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @admin_users.each do |admin_user| %>
        <tr>
          <td><%= link_to admin_user.id, admin_admin_user_path(admin_user) %></td>
          <td><%= admin_user.email %></td>
          <td><%= l(admin_user.created_at, format: :long) if admin_user.created_at %></td>
          <td class="text-nowrap">
            <%= link_to 'Edit', edit_admin_admin_user_path(admin_user), class: 'btn btn-link btn-sm' %>
            <%= button_to 'Delete', admin_admin_user_path(admin_user), method: :delete, class: 'btn btn-link btn-sm text-danger', form: { data: { turbo_confirm: 'Are you sure?' } } %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
